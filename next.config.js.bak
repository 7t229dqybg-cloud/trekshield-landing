/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "standalone",
  i18n: {
    locales: ['vi'],
    defaultLocale: 'vi',
    localeDetection: false,
  },
};

module.exports = nextConfig;
